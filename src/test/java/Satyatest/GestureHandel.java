package Satyatest;

import java.util.NoSuchElementException;

import org.openqa.selenium.By;
import org.openqa.selenium.SearchContext;
import org.openqa.selenium.WebElement;

import io.appium.java_client.AppiumDriver;

public class GestureHandel extends IndofastService{
	

 
         
	
	/* public static void GestureHandle(AppiumDriver driver, int maxScrolls) {
	    int scrollCount = 2;
	    while (scrollCount < maxScrolls) {
	        try {
	        	scrollUp(driver);
            if (driver.findElement(By.xpath("(//android.widget.FrameLayout[@resource-id=\'com.indofast.service.dev:id/cardView\'])[1]/android.widget.LinearLayout")).isDisplayed()) {
	                System.out.println("Element found!");
	                return;
	            }
	        } catch (NoSuchElementException e) {
	            
	            scrollCount++;
	        }
	    }
	    throw new RuntimeException("Element not found after scrolling " + maxScrolls + " times");
	}

	private static void scrollUp(AppiumDriver driver) {
		// TODO Auto-generated method stub   */
		
	}
	
	
	
	

